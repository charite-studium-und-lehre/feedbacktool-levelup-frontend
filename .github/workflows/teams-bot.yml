name: Teams deployment message
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        msg=$(git symbolic-ref --short HEAD)
        echo $msg
